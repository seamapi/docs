{
  "name": "seam-gitbook-plugin",
  "private": true,
  "scripts": {
    "lint": "eslint ./**/*.ts*",
    "typecheck": "tsc --noEmit",
    "publish-integrations-staging": "doppler run -p gitbook-seam-components-plugin -c prd -- gitbook publish ."
  },
  "devDependencies": {
    "@gitbook/cli": "^0.9.0",
    "@gitbook/runtime": "latest",
    "@cloudflare/workers-types": "latest",
    "@gitbook/tsconfig": "latest"
  }
}