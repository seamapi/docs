{{#each resources}}
<header>
  <h1>{{name}}</h1>
</header>

<hr>

{{#each properties}}
<header>
  <h3>{{name}}</h3>
</header>
<div>
  <p>{{description}}</p>
  {{#if format}}
  <div>
    <span>Format:</span>
    <span>{{format}}</span>
  </div>
  {{/if}}
  {{#if isDeprecated}}
  <p>This property is deprecated.</p>
  {{/if}}
</div>

<hr>
{{/each}}
{{/each}}
